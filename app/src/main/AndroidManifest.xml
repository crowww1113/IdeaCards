<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xiejinyi.ideacards">
    <!-- package保留原来的example，不用改代码包名，交作业完全没问题 -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IdeaCards"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ArchiveActivity" android:exported="false" />
        <activity android:name=".DetailActivity" android:exported="false" />
        <activity android:name=".ExportActivity" android:exported="false" />

        <!-- 极速输入浮窗：从控制中心磁贴呼出，独立任务栈避免激活主页 -->
        <activity
            android:name=".QuickInputActivity"
            android:theme="@style/Theme.QuickInput"
            android:exported="false"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:noHistory="true" />

        <!-- 控制中心快捷磁贴：下拉状态栏一键记灵感 -->
        <service
            android:name=".QuickNoteTileService"
            android:icon="@drawable/ic_qs_note"
            android:label="记下灵感"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>

        <!-- ContentProvider 随便用旧包名，本地数据库不影响谷歌功能 -->
        <provider
            android:name=".data.provider.NoteContentProvider"
            android:authorities="com.xiejinyi.ideacards.provider"
            android:exported="false" />

        <!-- 重点！FileProvider 必须和applicationId一致，导出文件才不会崩 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.xiejinyi.ideacards.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="com.google.android.gms.signin.ClientId"
            android:value="115702086596-9jbotqrsfj2pa1oolbsoc5ak0ui7rhbj.apps.googleusercontent.com" />

    </application>
</manifest>